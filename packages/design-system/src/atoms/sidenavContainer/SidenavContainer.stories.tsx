import type { ComponentStory } from '@storybook/react';
import SidenavContainer from './SidenavContainer';

export const Template: ComponentStory<typeof SidenavContainer> = (args) => <SidenavContainer {...args} />;

export const args = {
}
